1. 多Agent任务，Planer、Executor、Reviewer，需要加入任务校验者，在任务收尾后启动起来进行校验任务完成情况，然后反馈。需要有一个总调度agent，负责调用各个子agent完成任务，并接收和回复用户的消息。然后消息持久化这边就可以做下单独设计，比如子agent都是只是需要一段临时上下文进行执行，用户也不需要知道，这段内容也不需要给后续别的agent了解，唯有总调度agent需要保持任务的上下文，并把相关内容传递给各个子agent。需要你先设计架构，给出方案，记得各种agent间做好架构设计，比如接口\trait设计等。
