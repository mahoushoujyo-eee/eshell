1. 需要做出多Agent功能，改造现在的模式，Planer、Executor、Reviewer，需要加入任务校验者，在任务收尾后启动起来进行校验任务完成情况，然后反馈。需要有一个总调度agent，负责调用各个子agent完成任务，并接收和回复用户的消息。然后消息持久化这边就可以做下单独设计，比如子agent都是只是需要一段临时上下文进行执行，用户也不需要知道，这段内容也不需要给后续别的agent了解，唯有总调度agent需要保持任务的上下文，并把相关内容传递给各个子agent，但是为了方便调试等，需要你把子agent的接收与回复，以及中间的各个流程，都需要做日志或者持久化成一个个文件。需要你先设计架构，给出方案，记得各种agent间做好架构设计，比如接口\trait设计等。
2. 可以的话让前端可以感知现在的流程，比如planning、executing、reviewing、answering等等，然后也可以展示出一些相关内容。比如做的plan、执行的进度和情况等等
3. 新增agent相关配置，eshelldata里创建AGENTS.md文件维护一个全局的用户上下文信息，在每次对话都要做注入。同时要提供为每个server创建AGENTS.md文件维护，这个文件都存在客户端就行了，用户可以打开ai设置相关部分进行两个文件的设置
