//! Constants for the scripts domain.
//!
//! Currently empty by design: script storage derives everything from its
//! `Storage` paths (see `domain/config/consts.rs`) and has no domain-local
//! tuning knobs yet. Add a constant here when one appears.
